import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  wrapper: {
    alignSelf: 'stretch',
    flexDirection: 'row',
    flexWrap: 'wrap',
    marginVertical: 10,
  },
})

export default styles
