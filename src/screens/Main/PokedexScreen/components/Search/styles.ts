import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  search: {
    flex: 1,
    alignSelf: 'stretch',
    height: 46,
    textAlign: 'center',
    color: '#838282',
  },
})

export default styles
